@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@model WebApplication1.Models.Movie

<div class="container py-5">
    <div class="card shadow-sm mx-auto" style="max-width: 900px;">
        <div class="card-body">
            <h2 class="card-title text-center text-primary mb-4">🎬 Edit Movie</h2>

            <form asp-action="Edit" asp-controller="Movie" method="post" enctype="multipart/form-data">
                <input type="hidden" asp-for="Id" />

                <div class="row g-4">
                    <!-- Left side: current image -->
                    <div class="col-md-4 text-center">
                        <img src="@(string.IsNullOrEmpty(Model.ImageFile) ? "/images/placeholder.png" : "/images/" + Model.ImageFile)" 
                             alt="@Model.Name" 
                             class="img-fluid rounded mb-3" />
                    </div>

                    <!-- Right side: form inputs -->
                    <div class="col-md-8">
                        <!-- Movie Name -->
                        <div class="mb-3">
                            <label asp-for="Name" class="form-label">Movie Name</label>
                            <input asp-for="Name" class="form-control" />
                            <span asp-validation-for="Name" class="text-danger small"></span>
                        </div>

                        <!-- Genre -->
                        <div class="mb-3">
                            <label asp-for="GenreId" class="form-label">Genre</label>
                            <select asp-for="GenreId" asp-items="ViewBag.Genres" class="form-select">
                                <option value="">-- Select Genre --</option>
                            </select>
                            <span asp-validation-for="GenreId" class="text-danger small"></span>
                        </div>

                        <!-- Upload Image -->
                        <div class="mb-3">
                            <label class="form-label">Upload New Image</label>
                            <input type="file" name="ImageFile" class="form-control" />
                        </div>

                        <!-- Buttons -->
                        <div class="d-flex gap-2 mt-3">
                            <a asp-action="Index" class="btn btn-secondary">Cancel</a>
                            <button type="submit" class="btn btn-primary">Save Changes</button>
                        </div>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
<style>
    .card-title {
        font-size: 2rem;
        font-weight: 700;
    }